# Steps ManifestThis document lists **inputs** required by each step, indicating required fields, types, examples, and common outputs.Reuse outputs via `${steps.<id>.<field>}` or export them into `vars` in your flow.## Advanced Search (`advancedSearch`)**Inputs**- Required fields are marked with **(required)**.- **(required)** `advancedSearch.search_id` — Item ID — type: *string* — example: `ITEM-12345`- **(required)** `advancedSearch.advanced_query_type` — Advanced Query Type (index) — type: *number* — example: `1` — See catalog.json/advancedSearch.ADVANCED_QUERY_TYPE- **(required)** `advancedSearch.search_type` — Type (index) — type: *number* — example: `4` — See catalog.json/advancedSearch.SEARCH_TYPE**Common outputs**: `search_id`, `advanced_query_type`, `search_type`## Create Change Request (`createChanges`)**Inputs**- Required fields are marked with **(required)**.- **(required)** `createChanges.change_name` — Change Name — type: *string* — example: `CR 2025-08-19 12:30`- **(required)** `createChanges.description` — Description — type: *string* — example: `Auto created by pipeline`- **(required)** `createChanges.business_case` — Business Case — type: *string* — example: `BC demo`- **(required)** `createChanges.unplanned_change` — Unplanned Change (1-Yes 2-No) — type: *number* — example: `2`- **(required)** `createChanges.problem_change_type` — Problem/Change Type (index) — type: *number* — example: `7` — See catalog.json/createChanges.PROBLEM_CHANGE_TYPE- **(required)** `createChanges.primary_change_reason` — Primary Change Reason (index) — type: *number* — example: `12` — See catalog.json/createChanges.PRIMARY_CHANGE_REASON- **(required)** `createChanges.rpm_number` — RPM Attachment Number — type: *string* — example: `RPM-0001`**Common outputs**: `CR_number`## Derive Change (`deriveChange`)**Inputs**- Required fields are marked with **(required)**.- **(required)** `deriveChange.item_id` — Change ID — type: *id* — example: `${vars.crId}`**Common outputs**: `ID`## Create Mechanical Design (`createMechDesign`)**Inputs**- Required fields are marked with **(required)**.- **(required)** `createMechDesign.document_type` — Document Type (index) — type: *number* — example: `4` — See catalog.json/createMechDesign.DOCUMENT_TYPE- **(required)** `createMechDesign.document_subtype` — Document Subtype (index) — type: *number* — example: `2` — See catalog.json/createMechDesign.DOCUMENT_SUBTYPE[TYPE]- **(required)** `createMechDesign.sap_doc_type` — SAP Document Type (index) — type: *number* — example: `5` — See catalog.json/createMechDesign.SAP_DOC_TYPES- **(required)** `createMechDesign.molex_restriction` — Molex Restriction (index) — type: *number* — example: `2` — See catalog.json/createMechDesign.MOLEX_RESTRICTION- **(required)** `createMechDesign.name` — Name — type: *string* — example: `MD ${now:yyyyMMdd}`- **(required)** `createMechDesign.enable_molex_series` — Enable Molex Series (true/false or 1/2) — type: *boolean* — example: `true`- (optional) `createMechDesign.series_number` — Series Number (when enabled) — type: *string* — example: `SER-001`- **(required)** `createMechDesign.rpm_number` — RPM Attachment Number — type: *string* — example: `${vars.rpm1}`**Common outputs**: `created_id`, `name`## Create Product Part (`createProductPart`)**Inputs**- Required fields are marked with **(required)**.- **(required)** `createProductPart.item_name` — Item Name — type: *string* — example: `PP-01`- **(required)** `createProductPart.create_series` — Create Molex Series? (1-Yes 2-No) — type: *number* — example: `1`- (optional) `createProductPart.series_number` — Series Number (when enabled) — type: *string* — example: `SER-002`- **(required)** `createProductPart.rpm_number` — RPM Attachment Number — type: *string* — example: `RPM-0003`**Common outputs**: `created_id`, `ITEM_NAME`## Save As (`saveAs`)**Inputs**- Required fields are marked with **(required)**.- **(required)** `saveAs.source` — Source (1-Local 2-Repository) — type: *number* — example: `2`- **(required)** `saveAs.item_id` — Item ID to Save As — type: *id* — example: `${vars.mechId}`- **(required)** `saveAs.search_type` — Search Type (index) — type: *number* — example: `11` — See catalog.json/saveAs.SEARCH_TYPE- (optional) `saveAs.new_name` — New Name (Mech/Product cases) — type: *string* — example: `Copy of ${vars.mechName}`- (optional) `saveAs.molex_series` — Enable Molex Series? (1-Yes 2-No) — type: *number* — example: `1`- (optional) `saveAs.series_number` — Series Number (when enabled) — type: *string* — example: `SER-003`**Common outputs**: `created_id`, `NAME`## Submit to Workflow (`submitWorkflow`)**Inputs**- Required fields are marked with **(required)**.- **(required)** `submitWorkflow.template` — Workflow Template (index) — type: *number* — example: `2` — See catalog.json/submitWorkflow.WORKFLOW_TEMPLATES- **(required)** `submitWorkflow.item_id` — Item ID — type: *id* — example: `CR-XXXX`- **(required)** `submitWorkflow.username` — Participant Username — type: *string* — example: `xingyl4`**Common outputs**: `ID`, `WORKFLOW_TEMPLATE`## Run Workflow (complete/approve steps) (`runWorkflow`)**Inputs**- Required fields are marked with **(required)**.- **(required)** `runWorkflow.item_id` — Item ID — type: *id* — example: `CO-XXXX`- **(required)** `runWorkflow.priority` — Priority (index) — type: *number* — example: `2` — 1-Urgent 2-Standard- **(required)** `runWorkflow.business_impact` — Business Impact (index) — type: *number* — example: `2` — 1-Low 2-Medium 3-High- **(required)** `runWorkflow.plant_code` — Plant Code for PCN (index) — type: *number* — example: `3` — See catalog.json/runWorkflow.Plant_Code_for_PCN- **(required)** `runWorkflow.pcn_required` — PCN Required? (1-Yes 2-No) — type: *number* — example: `2`- **(required)** `runWorkflow.reason` — Reason for change — type: *string* — example: `Auto test`**Common outputs**: `ID`, `selected_priority`, `selected_impact`, `selected_plant`, `decision`